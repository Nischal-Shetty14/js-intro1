if(true)
{
    let a=10;
    const b=20;
    var c=30;
}
//console.log(a);
//console.log(b);
console.log(c);

//a and b don't print coz they are inside the block and not global.C should do the same thing but doesn't so that's why var is an issue 